{
  "name": "agent-analytics-webmcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A WebMCP evidence explorer for the OpenAI WebMCP Challenge.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
