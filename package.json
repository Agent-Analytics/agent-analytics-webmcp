{
  "name": "agent-analytics-webmcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A public, read-only WebMCP evidence explorer for Agent Analytics.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
